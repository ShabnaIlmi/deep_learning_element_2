# Python requirement: this project was developed for Python 3.9.10
# If you need to enforce this in tooling, add a `runtime.txt` or set the interpreter in your environment/CI.
# Core requirements
torch>=1.0.0,<2.0.0  # PyTorch 1.0+
numpy>=1.19.0        # Compatible with Python 3.5+
matplotlib>=3.0.0    # For plotting attention maps

# Version constraints to ensure Python 3.5+ compatibility